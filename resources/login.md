# Login

Login to Azure through the az cli with the `az login` command to authenticate.

If you don't have the az cli, you will need to [install](https://learn.microsoft.com/en-us/cli/azure/install-azure-cli) it.
